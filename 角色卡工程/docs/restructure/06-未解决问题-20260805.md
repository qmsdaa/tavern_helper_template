# Counterfeit 世界书重构 · 未解决问题清单

日期：2026-08-05 ｜ 按优先级排序；①②需用户决策，③-⑦为记录项

---

## ① 成品卡 PNG 未重新打包（阻塞生效）

所有修改目前只落在源文件（世界书/脚本源/store.ts）。当前 `Counterfeit-v0.4.6-20260805c.png` 及酒馆中的卡片**仍是旧内容**。需要执行打包（`tavern-cards-forge.mjs pack Counterfeit`，打包工具在 skill 目录）并回读验证后，重构才真正生效。**是否现在打包，待用户确认。**

## ② .build/readback-20260804 错字的成品卡状态未验证

打包回读中发现的"会组件学习""没有吧我理解你说出口"错字，**源文件已在此前修复**，但已分发的 PNG 成品卡是否含修复版未验证——需重新打包后以新回读为准。随①一并解决。

## ③ AGENTS.md 文件名笔误

AGENTS.md 写"故事规划.yaml"，实际文件名为 `创作规划.yaml`。建议下次维护时改正（本次未动 AGENTS.md）。

## ④ 未通读的次要文件

以下文件本次未逐行通读，如其中存在 scene/bond 成长依赖需后续补查：
- 四主角 `日程.yaml` ×4；
- NPC 其余条目（19 文件中未涉及星级删除的部分）；
- `EJS预处理/` 剩余文件：开局场景路由、开放世界在场注入、手机上下文注入、EJS预处理.txt。

## ⑤ "注"级场景未改文件（设计决策，非遗漏）

场景 5/10/11/22/31/33/35/37/49-52/55/60/80/88/94/96/102/106/109/130/138/145 等按重构计划为"注"级，主题由世界观与调色盘承载、事件全保留。若用户希望逐场也加★节拍行，可作为追加工作单执行。

## ⑥ `在场: true★` 原生约定保留

约 20 个场景文件、40 处 POV适配流式值含 `true★`（★为作者标重点用，YAML 解析为字符串但恒真值，运行无碍）。本次全部保留未动。若未来要求严格布尔，可一次性脚本化清洗（会失去"重点POV"标记信息，建议清洗前先确认无下游消费该标记）。

## ⑦ 任务书适配说明

任务书原定交付物按"JSON 工程"描述，已按世界书工程适配：备份目录（`cards/backups/worldbook-before-restructure-20260805/`）＋文档集（docs/restructure/ 01-06）。场景映射表即《03-场景主导权矩阵-20260805.md》（含 150 场逐场旧主导→新主导→改动级别→旗标域）。

## 附：本阶段新发现并即时修复的问题（已闭环）

- `脚本/Zod.txt` 缺 arc_milestones 块（schema.ts 已改、打包副本未同步）——**阶段D 已补**，否则会全员锁死第一阶段。
- `store.ts` INITIAL_RELATIONS 雪乃POV拉芙 romance=5 硬编码——**已改 0**。
- 场景九十三.yaml 重写引入 1 处 YAML 解析错误（行内未加引号引文）——**已修复复检通过**。
