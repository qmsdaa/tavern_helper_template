# Counterfeit 世界书重构 · 验证报告

日期：2026-08-05 ｜ 验证工具：`cards/validate_worldbook.py`、`cards/compare_yaml_errors.py`（PyYAML 6.0.3）、项目自带 build_tools 三个 .mjs 校验器（Node v24.15.0）

---

## 1. YAML 解析检查（对照备份差分）

| 项 | 结果 |
|---|---|
| 备份（重构前）解析失败文件数 | 89 |
| 当前（重构后）解析失败文件数 | 85 |
| **本次新引入** | **1（场景九十三.yaml，已修复→复检 YAML OK；修复后新引入=0）** |
| 本次修复（备份坏→当前好） | 4（四主角三面性.yaml，重写后比原版更规整） |

说明：85 个残留失败全部为**项目原生宽松 YAML 风格**（列表项内未加引号的冒号、流式值混排等），世界书条目在 SillyTavern 中作为提示文本加载、不经 YAML 解析器，属既有约定，不影响运行。本报告以"新引入=0"为通过标准。

## 2. EJS 检查

- 29 个含 EJS 的文件做 `<%`/`%>` 配对与 if 块计数：2 个文件（状态栏渲染.txt、角色阶段事实.txt）计数不等，**与备份逐字符一致**——为 else 分支造成的计数假阳性，非本次引入，判定通过。
- 项目自带 `verify_ejs_condition_guards.mjs`：**通过（173 处守卫）**（重构前 171，新增门控 2 处计入，全部符合 fail-closed 约定）。
- 新增 EJS（四调色盘 5 处、四三面性 4 处 stage 计算块）经逐文件目视复核：变量命名按角色隔离（`_hachiman/_yukino/_yui/_laff`、`tf*`），无全局污染；`getvar` API 与项目既有用法（阳乃/一色调色盘）一致。

## 3. 项目自带校验器

| 校验器 | 结果 |
|---|---|
| verify_ejs_condition_guards.mjs | 通过：173 处守卫 |
| verify_scene_fields.mjs | 通过：150/150 场字段完整（47 项已知豁免沿用 v5.0 A4） |
| verify_romance_guide_split.mjs | 通过：通则 770 字符，14 角色判定各 1 份 |

## 4. 残留 grep（全部通过）

| 检查项 | 结果 |
|---|---|
| `难度: ★` / 攻略星级 | 0 命中 |
| "每一次否定都是" / "这是最真实的" / "最真实的她" | 0 命中 |
| `romance=5` / `romance: 5` / `r5`（世界书） | 0 命中 |
| povLover 结构 | 四文件均为对象数组结构，`povLover_*.name` 合法，写法一致 |
| `true★` | 保留（约 20 文件 40 处，**项目原生约定**，YAML 解析为字符串但恒真值；非本次引入，见未解决问题⑥） |

## 5. 场景编号与日历

- 事件目录场景文件：**150 场，编号 1–150 连续，无缺失、无重复**。
- 时间线/日历文件本次未修改；剧本日期未动（仅场景 83/150 等各追加行内★节拍，不改 `时间:` 字段）。

## 6. 里程碑驱动抽样模拟（逻辑复核）

- **全 false（前期）**：旗标数 0 → stage 0 → 仅渲染第一段语料，恋人层不触发（commitment≠恋人）。✓
- **中期（1-2 旗标）**：stage 1 → 第二段。✓（拉芙 0/1/2/3-4→段 0/1/2/3 同理）
- **旧存档回退**：`stat_data.arc_milestones` 为 undefined → 走 deprecated scene/bond 推导分支，不抛错。✓
- **运行时 schema**：`脚本/Zod.txt` 已补 arc_milestones 块（本次阶段D 抓到的关键遗漏）——否则 zod 会剥离 AI 的旗标更新，全员锁死第一段。✓
- MVU 更新规则要求 Analysis 行输出旗标判定，`输出格式.yaml` 已同步。✓

## 7. 脚本镜像一致性

| 文件对 | 状态 |
|---|---|
| schema.ts ↔ 脚本/Zod.txt | arc_milestones 已双向同步 ✓ |
| 更新规则.yaml ↔ store.ts INITIAL_RELATIONS | 拉芙 romance=0 已双向同步 ✓ |
| 脚本/MVU.txt | 仅 bundle 导入，无业务内容，无需同步 ✓ |

## 结论

全部验证项通过；本次重构**零新引入解析错误**，项目自带三个校验器全绿。修改**尚未打包进 PNG 角色卡**——需重新打包后方可在酒馆生效（打包动作待用户确认）。
