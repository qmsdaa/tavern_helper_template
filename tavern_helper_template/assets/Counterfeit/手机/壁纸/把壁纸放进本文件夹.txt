把壁纸图片（jpg / png / webp，竖构图最佳）放进本文件夹，然后运行：

    python assets/tools/build_wallpapers.py --build

脚本会：
  1. 把图片压缩为 webp（最长边 1080px）写入「已压缩」子文件夹
  2. 生成手机壁纸清单数据（文件名即壁纸名）
  3. 自动执行 pnpm build

壁纸顺序 = 文件名自然排序（建议用 01、02、03… 开头命名）。
原图不会被改动；「已压缩」子文件夹里的内容可以随时删除重建。
